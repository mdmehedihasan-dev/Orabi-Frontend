/* eslint-disable react/prop-types */

const Button = ({title}) => {
  return (
    <button className="px-10 py-2 font-bold text-white lg:px-20 lg:py-4 bg-primaryColor font-dm ">{title}</button>
  )
}

export default Button